<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="view010101P01lite">
	
	<select id="retrieveWrkHistryList" parameterType="hashmap" resultType ="HashMap">

	    	/* retrieveWrkList */
	    	
			SELECT WRK_NM
				 , SOURCE
			     , TO_CHAR(CHNG_HISTRY_DTTM, 'YYYY-MM-DD HH24:MI:SS') AS CHNG_HISTRY_DTTM
			FROM ROBOT02
			WHERE USER_ID = #{userId}
			<if test='wrkNm != "" and wrkNm != null'>
			AND WRK_NM LIKE CONCAT('%', #{wrkNm}, '%')    
			</if>
			ORDER BY CHNG_HISTRY_DTTM DESC
	</select>


</mapper>